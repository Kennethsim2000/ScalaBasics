package com.rockthejvm

object FlatmapExample extends App {
    val lst = List("Hello", "World")
    val lstFlatten = lst.flatten // strings will be flattened to characters
    println(lstFlatten)

}
